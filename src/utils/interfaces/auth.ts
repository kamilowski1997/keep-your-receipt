export type UserDetailsType = {
  id: string;
  email: string;
};

export type RegisterData = {
  email: string;
  password: string;
};

export type LoginData = {
  email: string;
  password: string;
};
